# Demo-project
## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build

### Test with ESLINT  
`$ npm run lint`

### Test SCSS with scss-lint  
`$ gem install scss_lint`  
`$ npm run scss`